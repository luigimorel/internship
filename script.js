const name = "Luigi Morel";
//A console log to display the name
// console.log(name);

const courses = ["Design", "Front-end", "Coding"];
//A console log to display the courses
// console.log(courses);

//Since the number of courses are odd, I will find and display all odd numbers from 1-200 (inclusive)

for (let i = 0; i <= 200; i++) {
  if (i % 2 != 0) {
    console.log(i);
  }
}
a;
